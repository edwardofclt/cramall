/**
 * Placeholder so the registry covers `number-line-compare` today; Task 10 replaces the
 * innards with the real number-line comparison experiment.
 */
export default function NumberLineCompare() {
  return (
    <div className="card widget-placeholder" data-testid="widget-placeholder">
      <span aria-hidden="true">📏</span> Number line experiment loading soon!
    </div>
  );
}
