/**
 * Placeholder so the registry covers `place-value-builder` today; Task 10 replaces the
 * innards with the real drag-the-blocks manipulative.
 */
export default function PlaceValueBuilder() {
  return (
    <div className="card widget-placeholder" data-testid="widget-placeholder">
      <span aria-hidden="true">🔢</span> Place value experiment loading soon!
    </div>
  );
}
